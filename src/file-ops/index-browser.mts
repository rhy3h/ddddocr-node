export const isSupportDebug = () => {
    throw new Error("Not supported");
};

export const existsSync = () => false;

export const rmSync = () => {
    throw new Error("Not supported");
};

export const mkdirSync = () => {
    throw new Error("Not supported");
};

export const readFile = async () => {
    throw new Error("Not supported");
};

export const writeFile = async () => {
    throw new Error("Not supported");
};
